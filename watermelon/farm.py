import watermelon.button as button
import watermelon.widget as widget
import watermelon.tab_widget as tab_widget

widget_classes = set([
    button.Button,
    widget.Widget,
    tab_widget.TabWidget,
])
window_classes = set()
graphics_ob_classes = set()
ob_classes = set()          